#include<bits/stdc++.h>
using namespace std;

#define rep(i, N) for(int i = 0; i < N; i++)
#define mkp make_pair

using ll = long long;
const int INF = 1e9;
const ll INFL = 1e18;

int main() {
  char a, b, c;
  cin >> a >> b >> c;
  if (a == b && b == c) cout << "Won" << endl;
  else cout << "Lost" << endl;
}
