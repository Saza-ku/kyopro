#include<bits/stdc++.h>
using namespace std;

#define rep(i, N) for(int i = 0; i < N; i++)
#define mkp make_pair

using ll = long long;
const int INF = 1e9;
const ll INFL = 1e18;

int main() {
  cout<<fixed<<setprecision(14);
  float p, q, r;
  cin >> p >> q >> r;
  int min_x = floor(p - r - 3);
  int max_x = floor(p + r + 3); 
  int min_y = floor(q - r - 3);
  int max_y = floor(q + r + 3);
}
